---
phase: 08-ui-polish
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/components/gigs/GigCard.tsx
  - src/components/gigs/GigGrid.tsx
  - src/components/gigs/GigDetailView.tsx
  - src/components/gigs/GigImageGallery.tsx
  - src/components/gigs/PricingTierCard.tsx
  - src/components/gigs/ProviderCard.tsx
  - src/components/orders/OrderCard.tsx
  - src/components/orders/OrderStatusBadge.tsx
  - src/components/orders/OrderTimeline.tsx
  - src/components/chat/ChatInterface.tsx
  - src/components/chat/ConversationList.tsx
  - src/components/chat/MessageButton.tsx
  - src/components/chat/MessageInput.tsx
  - src/components/chat/MessageList.tsx
  - src/components/chat/TypingIndicator.tsx
  - src/components/reviews/StarRating.tsx
  - src/components/reviews/RatingInput.tsx
  - src/components/reviews/ReviewList.tsx
  - src/components/reviews/ReviewForm.tsx
  - src/components/profile/ProfileHeader.tsx
  - src/components/profile/ProfileAbout.tsx
  - src/components/profile/ProviderInfo.tsx
  - src/components/profile/PortfolioCarousel.tsx
  - src/components/search/SearchBar.tsx
  - src/components/search/FilterPanel.tsx
  - src/components/search/CategoryFilter.tsx
  - src/components/search/PriceRangeFilter.tsx
  - src/components/search/Pagination.tsx
  - src/app/gigs/page.tsx
  - src/app/gigs/[slug]/page.tsx
  - src/app/gigs/[slug]/not-found.tsx
  - src/app/gigs/[slug]/order/page.tsx
  - src/app/gigs/[slug]/order/OrderPageClient.tsx
  - src/app/gigs/new/page.tsx
  - src/app/gigs/[slug]/edit/page.tsx
  - src/app/orders/page.tsx
  - src/app/orders/[orderId]/page.tsx
  - src/app/messages/page.tsx
  - src/app/messages/[conversationId]/page.tsx
  - src/app/browse/[category]/page.tsx
  - src/app/u/[username]/page.tsx
  - src/app/u/[username]/not-found.tsx
autonomous: true

must_haves:
  truths:
    - "All gig components (card, detail, gallery, pricing) use burgundy instead of orange"
    - "All order components (card, badge, timeline) use burgundy accents"
    - "Chat interface uses burgundy theming for message bubbles and controls"
    - "Profile components use burgundy theming"
    - "Search/filter components use burgundy for active states"
    - "All listing/detail pages have cream backgrounds"
    - "Star ratings use burgundy-600 instead of amber/yellow"
    - "All pages have responsive layouts that work on mobile"
  artifacts:
    - path: "src/components/gigs/GigCard.tsx"
      provides: "Burgundy-themed gig card with hover effects"
      contains: "burgundy"
    - path: "src/components/gigs/PricingTierCard.tsx"
      provides: "Burgundy-themed pricing tier with popular highlight"
      contains: "burgundy"
    - path: "src/components/chat/MessageList.tsx"
      provides: "Burgundy-themed message bubbles"
      contains: "burgundy"
    - path: "src/components/search/SearchBar.tsx"
      provides: "Burgundy-themed search input"
      contains: "burgundy"
  key_links:
    - from: "src/app/gigs/[slug]/order/OrderPageClient.tsx"
      to: "sonner"
      via: "toast for order placement feedback"
      pattern: "import.*toast.*sonner"
    - from: "src/app/orders/[orderId]/page.tsx"
      to: "sonner"
      via: "toast for order action feedback"
      pattern: "toast"
---

<objective>
Retheme all remaining components and pages: gigs, orders, chat, reviews, profiles, search, and browse. Add toast feedback to order actions. Ensure responsive layouts.

Purpose: Completes the burgundy conversion across the entire application so no orange/amber remnants exist anywhere.
Output: Every component and page in the app uses burgundy theming consistently.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ui-polish/08-RESEARCH.md
@.planning/phases/08-ui-polish/08-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retheme gig, order, and chat components</name>
  <files>
    src/components/gigs/GigCard.tsx
    src/components/gigs/GigGrid.tsx
    src/components/gigs/GigDetailView.tsx
    src/components/gigs/GigImageGallery.tsx
    src/components/gigs/PricingTierCard.tsx
    src/components/gigs/ProviderCard.tsx
    src/components/orders/OrderCard.tsx
    src/components/orders/OrderStatusBadge.tsx
    src/components/orders/OrderTimeline.tsx
    src/components/chat/ChatInterface.tsx
    src/components/chat/ConversationList.tsx
    src/components/chat/MessageButton.tsx
    src/components/chat/MessageInput.tsx
    src/components/chat/MessageList.tsx
    src/components/chat/TypingIndicator.tsx
    src/components/reviews/StarRating.tsx
    src/components/reviews/RatingInput.tsx
    src/components/reviews/ReviewList.tsx
    src/components/reviews/ReviewForm.tsx
    src/components/profile/ProfileHeader.tsx
    src/components/profile/ProfileAbout.tsx
    src/components/profile/ProviderInfo.tsx
    src/components/profile/PortfolioCarousel.tsx
    src/components/search/SearchBar.tsx
    src/components/search/FilterPanel.tsx
    src/components/search/CategoryFilter.tsx
    src/components/search/PriceRangeFilter.tsx
    src/components/search/Pagination.tsx
  </files>
  <action>
    Apply systematic orange→burgundy color replacement across ALL listed component files.

    **Color mapping (same as Plan 02):**
    - `orange-*` / `amber-*` → `burgundy-*` (matching shade number)
    - Exception: `orange-600` / `amber-600` (primary) → `burgundy-800`
    - Exception: `orange-700` / `amber-700` (hover) → `burgundy-700`

    **Component-specific instructions:**

    **Gig components:**
    - `GigCard.tsx`: Card hover border: `hover:border-burgundy-300`. Price text: `text-burgundy-800`. "New" badge: `bg-burgundy-100 text-burgundy-800`. Card: `bg-white shadow-card hover:shadow-card-hover transition-all duration-200 rounded-xl`.
    - `GigGrid.tsx`: Ensure responsive grid: `grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6`
    - `GigDetailView.tsx`: All orange → burgundy. Ensure provider info section has `bg-cream-50` background. CTAs: `bg-burgundy-800 hover:bg-burgundy-700`.
    - `GigImageGallery.tsx`: Navigation dots/arrows in burgundy.
    - `PricingTierCard.tsx`: "Popular" highlight on Standard tier: `border-burgundy-800` with `bg-burgundy-50` header. "Popular" badge: `bg-burgundy-800 text-white`. Order button: `bg-burgundy-800 hover:bg-burgundy-700`.
    - `ProviderCard.tsx`: All orange → burgundy. Avatar border: `border-burgundy-200`. View profile link: `text-burgundy-700 hover:text-burgundy-800`.

    **Order components:**
    - `OrderCard.tsx`: All orange → burgundy. Keep semantic status colors (green=completed, yellow=in-progress, etc.) but change accent links to burgundy.
    - `OrderStatusBadge.tsx`: Keep status-specific colors unchanged (semantic meaning). Only change orange-themed defaults to burgundy.
    - `OrderTimeline.tsx`: Timeline line: `bg-burgundy-200`. Active dot: `bg-burgundy-800`. Completed dot: `bg-green-500` (keep semantic).

    **Chat components:**
    - `MessageList.tsx`: Own messages: `bg-burgundy-800 text-white`. Other messages: `bg-white`. All orange → burgundy.
    - `MessageInput.tsx`: Send button: `bg-burgundy-800 hover:bg-burgundy-700`. Input focus: `focus:ring-burgundy-500`.
    - `ConversationList.tsx`: Active conversation: `bg-burgundy-50 border-l-burgundy-800`. Unread indicator: `bg-burgundy-800`.
    - `ChatInterface.tsx`: Header: `bg-white border-b`. All orange → burgundy.
    - `MessageButton.tsx`: `bg-burgundy-800 hover:bg-burgundy-700 text-white`.
    - `TypingIndicator.tsx`: Dots use `bg-burgundy-400`.

    **Review components:**
    - `StarRating.tsx`: Filled stars: `text-burgundy-600` (NOT yellow/amber — matches warm aesthetic). Empty: `text-gray-300`.
    - `RatingInput.tsx`: Selected stars: `text-burgundy-600`. Hover: `text-burgundy-400`.
    - `ReviewList.tsx`: All orange → burgundy. Review cards: `bg-white shadow-soft rounded-lg`.
    - `ReviewForm.tsx`: Submit button burgundy. Toast on success: `toast.success('Review submitted!')`. Toast on error: `toast.error('Failed to submit review.')`. Add `import { toast } from 'sonner'`.

    **Profile components:**
    - `ProfileHeader.tsx`: All orange → burgundy. Provider badge: `bg-burgundy-100 text-burgundy-800`.
    - `ProfileAbout.tsx`: Section headers: `text-burgundy-900`.
    - `ProviderInfo.tsx`: All orange → burgundy. Skill badges: `bg-burgundy-50 text-burgundy-800`.
    - `PortfolioCarousel.tsx`: Navigation in burgundy.

    **Search components:**
    - `SearchBar.tsx`: Search icon: `text-burgundy-400`. Input focus: `focus:ring-burgundy-500 focus:border-burgundy-500`. Button: `bg-burgundy-800`.
    - `FilterPanel.tsx`: All orange → burgundy.
    - `CategoryFilter.tsx`: Active category: `bg-burgundy-800 text-white`. Inactive: `bg-cream-50 hover:bg-burgundy-50 text-burgundy-900`.
    - `PriceRangeFilter.tsx`: Range slider accent: `accent-burgundy-800`. Apply button: `bg-burgundy-800`.
    - `Pagination.tsx`: Active page: `bg-burgundy-800 text-white`. Hover: `hover:bg-burgundy-50`. Arrows: `text-burgundy-700`.

    **Add `transition-colors duration-200`** to all buttons and interactive elements that don't already have transitions.

    **Add rounded-xl to all card-type components** that currently use `rounded-lg` for warmer feel.
  </action>
  <verify>
    - `npm run build` passes
    - `grep -r "orange\|amber" src/components/` returns NO matches
  </verify>
  <done>All component files converted to burgundy. Zero orange/amber references remain in src/components/.</done>
</task>

<task type="auto">
  <name>Task 2: Retheme remaining pages and add order action toasts</name>
  <files>
    src/app/gigs/page.tsx
    src/app/gigs/[slug]/page.tsx
    src/app/gigs/[slug]/not-found.tsx
    src/app/gigs/[slug]/order/page.tsx
    src/app/gigs/[slug]/order/OrderPageClient.tsx
    src/app/gigs/new/page.tsx
    src/app/gigs/[slug]/edit/page.tsx
    src/app/orders/page.tsx
    src/app/orders/[orderId]/page.tsx
    src/app/messages/page.tsx
    src/app/messages/[conversationId]/page.tsx
    src/app/browse/[category]/page.tsx
    src/app/u/[username]/page.tsx
    src/app/u/[username]/not-found.tsx
  </files>
  <action>
    **Page-level styling updates for ALL listed pages:**

    1. All page backgrounds: Replace any `bg-gradient-to-br from-orange-*` or `bg-white` body backgrounds with `bg-cream-50 min-h-screen`
    2. All page containers: Ensure `max-w-*xl mx-auto px-4 sm:px-6 py-8 sm:py-12` pattern for responsive padding
    3. All orange/amber classes → burgundy equivalents using the same mapping as before
    4. All page headings: `text-burgundy-900`
    5. All section cards: `bg-white shadow-card rounded-xl`

    **Specific page instructions:**

    - `src/app/gigs/page.tsx`: Background `bg-cream-50`. Search area: clean spacing.
    - `src/app/gigs/[slug]/page.tsx`: Background `bg-cream-50`. All orange → burgundy.
    - `src/app/gigs/[slug]/not-found.tsx`: Use EmptyState component from `@/components/ui/EmptyState` with appropriate icon (ExclamationTriangleIcon from @heroicons/react/24/outline), title "Gig not found", description, and action to browse gigs.
    - `src/app/gigs/new/page.tsx`: Background `bg-cream-50`. All orange → burgundy.
    - `src/app/gigs/[slug]/edit/page.tsx`: Background `bg-cream-50`. All orange → burgundy.

    - `src/app/gigs/[slug]/order/page.tsx`: Background `bg-cream-50`. All orange → burgundy.
    - `src/app/gigs/[slug]/order/OrderPageClient.tsx`: Replace all orange → burgundy. Modal: `bg-white shadow-card-hover rounded-xl`. Confirm button: `bg-burgundy-800 hover:bg-burgundy-700`. Add toast feedback: `toast.success('Order placed successfully!')` on success, `toast.error('Failed to place order.')` on error. Add loading state to confirm button.

    - `src/app/orders/page.tsx`: Background `bg-cream-50`. All orange → burgundy.
    - `src/app/orders/[orderId]/page.tsx`: Background `bg-cream-50`. All orange → burgundy. For order action buttons (accept, start work, complete, cancel), add `import { toast } from 'sonner'` and show toast after each action: `toast.success('Order accepted!')`, `toast.success('Work started!')`, `toast.success('Order completed!')`, `toast.success('Order cancelled.')`. Add loading states to each action button — use useState for each action's loading state, show spinner and disable during processing.

    - `src/app/messages/page.tsx`: Background `bg-cream-50`. All orange → burgundy.
    - `src/app/messages/[conversationId]/page.tsx`: Background `bg-cream-50`. All orange → burgundy.
    - `src/app/browse/[category]/page.tsx`: Background `bg-cream-50`. All orange → burgundy. Category header: `text-burgundy-900`.
    - `src/app/u/[username]/page.tsx`: Background `bg-cream-50`. All orange → burgundy.
    - `src/app/u/[username]/not-found.tsx`: Use EmptyState component with UserIcon, title "User not found".

    **Responsive improvements across all pages:**
    - Ensure all grids use `grid-cols-1 md:grid-cols-2 lg:grid-cols-3` pattern
    - Padding: `px-4 sm:px-6` (mobile-first)
    - Text sizes: headings scale down on mobile (use `text-2xl sm:text-3xl`)
    - Button spacing: `gap-2 sm:gap-3`
    - Stack horizontally-arranged elements vertically on mobile using `flex-col sm:flex-row`
  </action>
  <verify>
    - `npm run build` passes
    - `grep -r "orange\|amber" src/app/` returns NO matches across entire app directory
    - `grep -r "orange\|amber" src/components/` returns NO matches
    - Order detail page imports toast from sonner
  </verify>
  <done>All remaining pages converted to burgundy with cream backgrounds. Order actions show toast notifications. Not-found pages use EmptyState component. Responsive padding and layouts applied.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- Zero orange/amber references anywhere in src/ directory
- All pages have cream-50 backgrounds
- All cards use shadow-card with rounded-xl
- Order actions show toast feedback
- Not-found pages use EmptyState component
- Responsive layouts work on mobile (px-4 base, scaling up)
</verification>

<success_criteria>
Every component and page in the entire application uses burgundy theming consistently. No trace of orange/amber remains. Order actions provide toast feedback. Responsive layouts are applied throughout.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ui-polish/08-03-SUMMARY.md`
</output>
