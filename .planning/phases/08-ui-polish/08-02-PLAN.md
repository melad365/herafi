---
phase: 08-ui-polish
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/app/(auth)/layout.tsx
  - src/app/(auth)/login/page.tsx
  - src/app/(auth)/register/page.tsx
  - src/app/dashboard/page.tsx
  - src/app/profile/edit/page.tsx
  - src/app/provider/setup/page.tsx
  - src/app/provider/dashboard/page.tsx
  - src/app/provider/dashboard/_components/GigsTab.tsx
  - src/app/provider/dashboard/_components/OrdersTab.tsx
  - src/app/provider/dashboard/_components/MessagesTab.tsx
  - src/components/provider/TabNavigation.tsx
  - src/components/forms/ProfileEditForm.tsx
  - src/components/forms/ProviderSetupForm.tsx
  - src/components/forms/GigForm.tsx
  - src/components/forms/PricingTierInput.tsx
  - src/components/forms/ImageUploadSection.tsx
  - src/components/forms/AvatarCropModal.tsx
autonomous: true

must_haves:
  truths:
    - "Auth pages (login, register) use burgundy theming instead of orange/amber"
    - "Dashboard page uses burgundy accents and cream backgrounds instead of orange gradients"
    - "Provider dashboard uses burgundy theming throughout"
    - "All form components use burgundy-themed buttons and inputs"
    - "All submit buttons show loading spinners during form submission"
  artifacts:
    - path: "src/app/(auth)/login/page.tsx"
      provides: "Burgundy-themed login page"
      contains: "burgundy"
    - path: "src/app/dashboard/page.tsx"
      provides: "Burgundy-themed buyer dashboard"
      contains: "burgundy"
    - path: "src/app/provider/dashboard/page.tsx"
      provides: "Burgundy-themed provider dashboard"
      contains: "burgundy"
  key_links:
    - from: "src/components/forms/ProfileEditForm.tsx"
      to: "sonner"
      via: "toast import for form submission feedback"
      pattern: "import.*toast.*sonner"
    - from: "src/components/forms/GigForm.tsx"
      to: "sonner"
      via: "toast import for form submission feedback"
      pattern: "import.*toast.*sonner"
---

<objective>
Retheme all auth pages, dashboards, and form components from orange/amber to burgundy. Add toast notifications and button loading states to all forms.

Purpose: Converts the two highest-traffic flows (authentication and dashboard) plus all forms to the new burgundy design system.
Output: All auth, dashboard, and form pages use burgundy theming with toast feedback and loading states.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ui-polish/08-RESEARCH.md
@.planning/phases/08-ui-polish/08-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retheme auth pages and dashboards</name>
  <files>
    src/app/(auth)/layout.tsx
    src/app/(auth)/login/page.tsx
    src/app/(auth)/register/page.tsx
    src/app/dashboard/page.tsx
    src/app/provider/dashboard/page.tsx
    src/app/provider/dashboard/_components/GigsTab.tsx
    src/app/provider/dashboard/_components/OrdersTab.tsx
    src/app/provider/dashboard/_components/MessagesTab.tsx
    src/components/provider/TabNavigation.tsx
  </files>
  <action>
    Perform a systematic color replacement across all listed files. This is NOT a simple find-replace — each file needs visual coherence.

    **Color mapping rules (apply consistently):**
    - `orange-50` / `amber-50` → `burgundy-50`
    - `orange-100` / `amber-100` → `burgundy-100`
    - `orange-200` / `amber-200` → `burgundy-200`
    - `orange-300` → `burgundy-300`
    - `orange-400` → `burgundy-400`
    - `orange-500` → `burgundy-500`
    - `orange-600` / `amber-600` → `burgundy-800` (primary action color)
    - `orange-700` / `amber-700` → `burgundy-700` (hover state)
    - `orange-800` / `amber-800` → `burgundy-800`
    - `orange-900` / `amber-900` → `burgundy-900`

    **Background mapping:**
    - `bg-gradient-to-br from-orange-50 via-amber-50 to-orange-100` → `bg-cream-50` (flat warm background, no gradient)
    - `bg-white` on cards → keep `bg-white` (cards stay white for contrast against cream)
    - `bg-gray-50` → `bg-cream-50` for subtle section backgrounds

    **Shadow upgrades on cards:**
    - `shadow-lg` on cards → `shadow-card` (custom shadow)
    - Add `hover:shadow-card-hover transition-shadow` to interactive cards

    **Specific file instructions:**

    1. `src/app/(auth)/layout.tsx` — Replace orange gradient background with `bg-cream-50 min-h-screen`

    2. `src/app/(auth)/login/page.tsx` — Replace all orange/amber classes with burgundy equivalents. Form card: `bg-white shadow-card rounded-xl`. Submit button: use burgundy-800 with hover:burgundy-700. Links: `text-burgundy-700 hover:text-burgundy-800`. Error messages: keep red styling.

    3. `src/app/(auth)/register/page.tsx` — Same treatment as login page.

    4. `src/app/dashboard/page.tsx`:
       - Page background: `bg-cream-50 min-h-screen`
       - Provider badge: `bg-burgundy-100 text-burgundy-800`
       - Username setup prompt: `bg-burgundy-50 border-burgundy-200`, text uses `text-burgundy-900` / `text-burgundy-800`
       - All orange links/buttons → burgundy-800 (primary) or burgundy-700 (hover)
       - Navigation cards: Provider Dashboard card gets `bg-gradient-to-br from-burgundy-50 to-cream-100 border-burgundy-200 hover:border-burgundy-400`, text `text-burgundy-900`
       - Become a Provider card: same gradient as Provider Dashboard card
       - Other nav cards: `bg-white border-gray-200 hover:border-burgundy-300`
       - Sign out button: `bg-burgundy-800 hover:bg-burgundy-700`

    5. `src/app/provider/dashboard/page.tsx` — Replace all orange/amber with burgundy. Stats use `text-burgundy-800`. Header: `bg-white shadow-card rounded-xl`. Stat cards: subtle borders.

    6. `src/app/provider/dashboard/_components/GigsTab.tsx` — All orange classes → burgundy. Active badge stays green. Edit button: `bg-burgundy-100 text-burgundy-700 hover:bg-burgundy-200`.

    7. `src/app/provider/dashboard/_components/OrdersTab.tsx` — All orange → burgundy. Keep order status color coding (green for completed, yellow for in-progress, etc.).

    8. `src/app/provider/dashboard/_components/MessagesTab.tsx` — All orange → burgundy.

    9. `src/components/provider/TabNavigation.tsx` — Active tab: `text-burgundy-800 border-burgundy-800`. Hover: `hover:text-burgundy-600`.

    **Add `transition-colors duration-200` to all interactive elements** (links, buttons) that don't already have transitions.
  </action>
  <verify>
    - `npm run build` passes
    - `grep -r "orange\|amber" src/app/\(auth\) src/app/dashboard src/app/provider src/components/provider` returns NO matches (all converted)
  </verify>
  <done>Auth pages, buyer dashboard, and provider dashboard fully themed with burgundy. No orange/amber classes remain in these files.</done>
</task>

<task type="auto">
  <name>Task 2: Retheme forms and add toast/loading feedback</name>
  <files>
    src/components/forms/ProfileEditForm.tsx
    src/components/forms/ProviderSetupForm.tsx
    src/components/forms/GigForm.tsx
    src/components/forms/PricingTierInput.tsx
    src/components/forms/ImageUploadSection.tsx
    src/components/forms/AvatarCropModal.tsx
    src/app/profile/edit/page.tsx
    src/app/provider/setup/page.tsx
  </files>
  <action>
    **Color replacement in all form files:**
    Apply same orange→burgundy mapping as Task 1. Focus on:
    - Submit buttons: `bg-burgundy-800 hover:bg-burgundy-700 text-white`
    - Cancel/secondary buttons: `bg-cream-100 text-burgundy-900 hover:bg-cream-50`
    - Input focus rings: `focus:ring-burgundy-500 focus:border-burgundy-500`
    - Active/selected states: `border-burgundy-800` or `bg-burgundy-50`
    - Labels: `text-burgundy-900` or `text-gray-700`
    - Helper text: `text-gray-500`

    **Toast notifications — add to ALL form submissions:**
    Import `import { toast } from 'sonner'` in each client component that handles form submission.

    1. `ProfileEditForm.tsx`:
       - On successful save: `toast.success('Profile updated successfully!')`
       - On error: `toast.error('Failed to update profile. Please try again.')`
       - Add `isLoading` state, set true during submission, pass to submit button
       - Submit button: show spinner + "Saving..." when loading, disabled when loading
       - Replace all orange/amber classes

    2. `ProviderSetupForm.tsx`:
       - On success: `toast.success('Provider account activated!')`
       - On error: `toast.error('Failed to set up provider account.')`
       - Add loading state to submit button
       - Replace all orange/amber classes

    3. `GigForm.tsx`:
       - On create success: `toast.success('Gig created successfully!')`
       - On update success: `toast.success('Gig updated!')`
       - On error: `toast.error('Failed to save gig. Please try again.')`
       - Add loading state to submit button
       - Replace all orange/amber classes

    4. `PricingTierInput.tsx` — Replace orange → burgundy. Toggle switches use burgundy. Active tier border: `border-burgundy-800`.

    5. `ImageUploadSection.tsx` — Replace orange → burgundy. Upload button: burgundy styling. Delete button: keep red. Drag area border: `border-burgundy-300` on drag.

    6. `AvatarCropModal.tsx` — Replace orange → burgundy. Confirm button: `bg-burgundy-800`. Cancel: `bg-gray-200`.

    7. `src/app/profile/edit/page.tsx` — Replace any orange background with `bg-cream-50`.

    8. `src/app/provider/setup/page.tsx` — Replace orange → burgundy background and accents.

    **Button loading pattern for each form:**
    ```tsx
    const [isSubmitting, setIsSubmitting] = useState(false);

    const handleSubmit = async (formData: FormData) => {
      setIsSubmitting(true);
      try {
        const result = await serverAction(formData);
        if (result?.error) {
          toast.error(result.error);
        } else {
          toast.success('Success message');
        }
      } catch (err) {
        toast.error('Something went wrong.');
      } finally {
        setIsSubmitting(false);
      }
    };
    ```

    Apply this pattern to each form, adapting to its existing submission mechanism (useActionState, form action, etc.). Do NOT break existing form logic — only wrap it with loading state and toast feedback.
  </action>
  <verify>
    - `npm run build` passes
    - `grep -r "orange\|amber" src/components/forms src/app/profile src/app/provider/setup` returns NO matches
    - Each form component imports `toast` from `sonner`
  </verify>
  <done>All form components themed with burgundy. Every form submission shows toast notification on success/error and displays inline spinner on submit button during processing.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- Zero orange/amber references in auth, dashboard, provider, and form files
- All form submissions trigger toast notifications
- All submit buttons show loading spinners during processing
- Cards use shadow-card with hover effects
- Interactive elements have transition-colors duration-200
</verification>

<success_criteria>
Auth pages, both dashboards, and all forms are fully converted to burgundy theming with warm cream backgrounds. Every form provides toast feedback and button loading states for all submissions.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ui-polish/08-02-SUMMARY.md`
</output>
